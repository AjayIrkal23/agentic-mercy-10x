# Debt quantification — cost in dollars (jcodemunch-backed inputs)

> Absorbed into `tech-debt-audit`. Translates code problems into ROI / dollars for executives and quality budgets (2-3x defect, productivity-multiplier formulas).

Source the metrics from `mcp__jcodemunch__get_hotspots`, `get_coupling_metrics`, `get_repo_health`, and `get_symbol_complexity`; apply the research-backed cost formulas in-process. No POSIX-only shell arithmetic pipelines.
