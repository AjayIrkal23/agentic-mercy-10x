# The skill graph and its stories

See `using-agent-skills/references/graph-and-stories.md`. Clusters = canonical + aliases + sidecar members; the index + alias map + overrides encode the graph the router ranks over.
