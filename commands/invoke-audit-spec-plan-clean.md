---
description: Orchestrate the AUDIT + SPEC + PLAN + CLEANUP flow — intel brief -> dispatch audit-specialist, spec-architect, planning-director, deadcode-reaper -> auto-close (31-skill legacy fallback inline)
---
<!-- AUTO-GENERATED by ~/.claude/hooks/gen-invoke-commands.py from autonomous-skill-router.config.json — do not edit by hand; edit the config and re-run the generator. -->

This is the orchestrated **AUDIT + SPEC + PLAN + CLEANUP** suite (3-act). Execute the acts IN ORDER, top to bottom, without asking for permission except at marked CHECKPOINTs. Artifacts are files on disk — a broken chain resumes from the last artifact instead of restarting.

## ACT A — INTEL (do this yourself, no dispatch)

1. jcodemunch: `plan_turn` (query = the user's request) + `assemble_task_context` on the impacted area.
2. graphify: `graph_stats` + `query_graph` for the impacted area (freshness check; rebuild only if a guard reported STALE).
3. Read `CODEX.md` + the repo root `CLAUDE.md` if present (dox tree: root → target dir).
4. Produce the **BRIEF**: task, surfaces (FE/BE/infra), impacted layers, contracts at risk, relevant paths.

Every agent dispatched below receives this BRIEF — specialists start warm, never blind.

## ACT B — DISPATCH (in this order)

### AUDIT → audit-specialist

Spawn agent **audit-specialist** (Agent tool, subagent_type "audit-specialist", description prefix `[sonnet] `, model:"sonnet") with: the BRIEF + its reading list (the agent file already carries it; the same skills appear in the FALLBACK block below).
Wait for artifact `AUDIT-*.md`.

### SPEC → spec-architect

Spawn agent **spec-architect** (Agent tool, subagent_type "spec-architect", description prefix `[sonnet] `, model:"sonnet") with: the BRIEF + the prior act's artifact (`AUDIT-*.md`) + its reading list (the agent file already carries it; the same skills appear in the FALLBACK block below).
Wait for artifact `SPEC-*.md`.
**CHECKPOINT:** show the user a compact summary of the artifact and get approval before continuing to the next act.

### PLAN → planning-director

Spawn agent **planning-director** (Agent tool, subagent_type "planning-director", description prefix `[sonnet] `, model:"sonnet") with: the BRIEF + the prior act's artifact (`SPEC-*.md`) + its reading list (the agent file already carries it; the same skills appear in the FALLBACK block below).
Wait for artifact `plan-*.md`.
**CHECKPOINT:** show the user a compact summary of the artifact and get approval before continuing to the next act.

### CLEANUP → deadcode-reaper

Spawn agent **deadcode-reaper** (Agent tool, subagent_type "deadcode-reaper", description prefix `[sonnet] `, model:"sonnet") with: the BRIEF + the prior act's artifact (`plan-*.md`) + its reading list (the agent file already carries it; the same skills appear in the FALLBACK block below).
Wait for artifact `REAP-REPORT.md`.

## FALLBACK — legacy inline mode

Use ONLY if agent dispatch is unavailable (offline, subagent limits, Agent tool missing). Degrade gracefully: invoke the skills below yourself via the Skill tool, in order, and execute every act sequentially in this context. Never silently drop a category.

**[AUDIT]** skills:
- `tech-debt-audit`
- `forensic-hotspot-finder`
- `forensic-change-coupling`
- `forensic-complexity-trends`
- `forensic-debt-quantification`
- `dead-code-and-change-audit`
- `improve-codebase-architecture`
- `code-review-and-quality`
- `architect-system-design`
- `codebase-start-point-guide`
- `project-reference-linkage`
- `project-structure-map`
- `mcp-usage-standards`
- `iterative-retrieval`

_Superpowers:_
- `superpowers:requesting-code-review`
- `superpowers:receiving-code-review`
- `superpowers:verification-before-completion`

**[SPEC]** skills:
- `spec-driven-development`
- `api-and-interface-design`
- `planning-and-task-breakdown`

_Superpowers:_
- `superpowers:brainstorming`
- `superpowers:writing-plans`

**[PLAN]** skills:
- `plan-mode-gate`
- `workflow-orchestrator`
- `plan-exec-stack-guide`
- `idea-refine`

_Superpowers:_
- `superpowers:dispatching-parallel-agents`

**[CLEANUP]** skills:
- `code-simplification`
- `fix-lint-format`
- `deprecation-and-migration`

_Superpowers:_
- `superpowers:finishing-a-development-branch`

Apply the orchestration above to the user's request:

$ARGUMENTS
