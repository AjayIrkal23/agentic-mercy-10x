---
description: Upstream skill sync — check .provenance.json sources, overlay-merge updates, report diff
---
<!-- AUTO-GENERATED by ~/.claude/hooks/gen-invoke-commands.py from autonomous-skill-router.config.json — do not edit by hand; edit the config and re-run the generator. -->

Upstream skill sync. Follow the P1 sync contract (HOME-WORKFLOW-DETAILS/UPGRADE-PLAN/PHASES.md, Phase P1).

1. Read `~/.claude/skills/.provenance.json` — the source-of-truth inventory (origin, channel, version/commit per skill).
2. For each non-self channel (plugin, gsd, gstack, raw-copy): check the upstream source for updates (plugin manager, git fetch, or the recorded fetch command).
3. Overlay-merge updates WITHOUT clobbering local overlays; snapshot every replaced version to `~/.claude/attic/<today>/skills-pre-update/` first.
4. Update `.provenance.json` entries for anything refreshed.
5. Report the diff: updated / skipped (already current) / conflicts (local overlay vs upstream change), one line per skill.

$ARGUMENTS
