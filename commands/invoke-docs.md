---
description: Dispatch docs-sync-agent standalone -> DOCS-SYNC-REPORT.md
---
<!-- AUTO-GENERATED by ~/.claude/hooks/gen-invoke-commands.py from autonomous-skill-router.config.json — do not edit by hand; edit the config and re-run the generator. -->

Dispatch the **docs-sync-agent** specialist NOW (Agent tool, subagent_type "docs-sync-agent", description prefix `[sonnet] `, model:"sonnet") — standalone run.

1. Quick intel (yourself, no dispatch): list the code files changed this session/branch (`git diff --name-only`) and the doc trees the repo carries (CLAUDE.md tree, server_docs/, frontend_docs/, CODEX.md).
2. Spawn **docs-sync-agent** with that scope as its brief. Its job: Phase-7 doc sync — CLAUDE.md tree, server_docs/frontend_docs, CODEX.
3. Wait for `DOCS-SYNC-REPORT.md`, then relay a compact summary of it to the user with the artifact path.

FALLBACK — if agent dispatch is unavailable, do the work inline following the agent file's workflow (~/.claude/agents/docs-sync-agent.md). Never skip the report.

Apply to:

$ARGUMENTS
