---
description: Dispatch qa-verifier standalone -> VERIFY-REPORT.md
---
<!-- AUTO-GENERATED by ~/.claude/hooks/gen-invoke-commands.py from autonomous-skill-router.config.json — do not edit by hand; edit the config and re-run the generator. -->

Dispatch the **qa-verifier** specialist NOW (Agent tool, subagent_type "qa-verifier", description prefix `[sonnet] `, model:"sonnet") — standalone run.

1. Quick intel (yourself, no dispatch): identify the current diff/branch scope (`git status`, `git diff --stat`, recent commits) and what behavior it claims to ship.
2. Spawn **qa-verifier** with that scope as its brief. Its job: end-to-end functional verification of what shipped.
3. Wait for `VERIFY-REPORT.md`, then relay a compact summary of it to the user with the artifact path.

FALLBACK — if agent dispatch is unavailable, do the work inline following the agent file's workflow (~/.claude/agents/qa-verifier.md). Never skip the report.

Apply to:

$ARGUMENTS
